name: First Workflow

on:
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print a welcome message 
        run: echo "welcome to our first Github Actions workflow!!"

      - name: Checkout repository
        uses: actions/checkoust@5.0.0
        
      - name: List repository files 
        run: ls -ls
